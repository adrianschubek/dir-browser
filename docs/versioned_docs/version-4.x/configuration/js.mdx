# Custom JavaScript

import EnvConfig from '@site/src/components/EnvConfig';

<EnvConfig name="JS|JS_URL|JS_URL_ONCE" init="<empty>|<empty>|<empty>" values="<code>|<URL>|<URL>" versions="3.3|3.3|3.3" desc="JS code. Will be executed on every page navigated.|URL to JS file. Will be executed on every page navigated.|URL to JS file. Will be executed only once on first visit. Subsequent navigation will not execute it again."/>